const defaultSize = 16

export default {
  avatarSizes: {
    extraSmall: defaultSize * 2.5,
    small     : defaultSize * 3,
    medium    : defaultSize * 4,
    large     : defaultSize * 6,
    extraLarge: defaultSize * 10,
  },
}
